'use strict';
export const $=selector=>document.querySelector(selector);export const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));export const pad=value=>String(value).padStart(2,'0');const STORE='campus-servicedesk-course-v8';
export const runtime={course:[],schedule:[],state:{group:'',done:{},steps:{}}};try{const saved=JSON.parse(localStorage.getItem(STORE)||'{}');if(saved&&typeof saved==='object')runtime.state={...runtime.state,...saved};}catch{}
export function save(){try{localStorage.setItem(STORE,JSON.stringify(runtime.state));}catch{}updateProgressPill();}
export function inline(value){let text=esc(value);text=text.replace(/`([^`]+)`/g,'<code>$1</code>');return text.replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>');}
export function block(value){const source=String(value||'').trim();if(!source)return'';const chunks=[];let rest=source;while(rest.includes('```')){const start=rest.indexOf('```'),end=rest.indexOf('```',start+3);if(end<0)break;chunks.push({type:'text',value:rest.slice(0,start)});chunks.push({type:'code',value:rest.slice(start+3,end).replace(/^\w+\n/,'').trim()});rest=rest.slice(end+3);}chunks.push({type:'text',value:rest});return chunks.map(x=>x.type==='code'?`<pre class="command">${esc(x.value)}</pre>`:x.value.split(/\n{2,}/).filter(Boolean).map(p=>p.trim().startsWith('>')?`<blockquote class="question">${inline(p.replace(/^>\s?/gm,'').trim())}</blockquote>`:`<p>${inline(p.trim()).replace(/\n/g,'<br>')}</p>`).join('')).join('');}
export function todayMoscow(){return new Intl.DateTimeFormat('sv-SE',{timeZone:'Europe/Moscow',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());}export function dateLabel(v){return new Date(`${v}T12:00:00+03:00`).toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'});}export const routeName=()=>location.hash.slice(1)||'home';export const currentGroup=()=>runtime.schedule.find(x=>x.group===runtime.state.group)||runtime.schedule[0];
export function doneSet(){const list=runtime.state.done?.[runtime.state.group];return new Set(Array.isArray(list)?list:[]);}export function setDone(number,isDone){const set=doneSet();isDone?set.add(number):set.delete(number);runtime.state.done||={};runtime.state.done[runtime.state.group]=[...set].sort((a,b)=>a-b);save();}export function taskSteps(number){const group=runtime.state.steps?.[runtime.state.group]||{};return new Set(Array.isArray(group[number])?group[number]:[]);}export function setTaskStep(number,index,checked){runtime.state.steps||={};runtime.state.steps[runtime.state.group]||={};const set=taskSteps(number);checked?set.add(index):set.delete(index);runtime.state.steps[runtime.state.group][number]=[...set].sort((a,b)=>a-b);save();}
export function updateProgressPill(){const node=$('#progress-pill');if(node)node.textContent=`${doneSet().size} / ${runtime.course.length||6} complete`;}
export function nearestLesson(){const group=currentGroup(),today=todayMoscow();const requiredIndex=group.dates.findIndex(d=>d>=today),nextRequired=requiredIndex>=0?group.dates[requiredIndex]:null;if(group.intro?.date>=today&&(!nextRequired||group.intro.date<=nextRequired))return{group,lesson:null,intro:group.intro,date:group.intro.date,index:-1};let index=requiredIndex;if(index<0)index=group.dates.length-1;return{group,lesson:runtime.course[index],intro:null,date:group.dates[index],index};}
export function taskCard(item){if(item.locked)return`<article class="task-card panel locked"><div class="task-card-top"><span class="task-number">Stage ${pad(item.number)}</span><span class="status">OPENS LATER</span></div><h3>${esc(item.title)}</h3><p>${esc(item.ability)}</p><div class="task-artifact"><b>When does it open?</b>${esc(item.releaseHint||'After the previous stage is complete.')}</div></article>`;const done=doneSet().has(item.number);return`<article class="task-card panel ${done?'done':''}"><div class="task-card-top"><span class="task-number">Stage ${pad(item.number)}</span><span class="status ${done?'done':''}">${done?'COMPLETE':'OPEN'}</span></div><h3>${esc(item.title)}</h3><p>${esc(item.goal||item.ability)}</p><div class="task-artifact"><b>What you submit</b>${esc(item.artifact)}</div><div class="actions"><a class="button primary" href="#task-${item.number}">Open stage</a><a class="button starter-download" href="#" data-starter="${esc(item.downloads.starterKey)}" data-filename="${esc(item.downloads.starterFilename)}">Backup project</a></div></article>`;}
