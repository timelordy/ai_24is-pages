'use strict';
import {runtime,esc,pad,inline,block,doneSet,setDone,taskSteps,setTaskStep} from './lib.js';
function lessonNav(active){return`<nav class="task-nav panel" aria-label="Project stages">${runtime.course.map(item=>item.locked?`<span class="locked-nav"><span>${pad(item.number)}</span><b>${esc(item.title)}</b></span>`:`<a class="${item.number===active?'active':''}" href="#task-${item.number}"><span>${pad(item.number)}</span><b>${esc(item.title)}</b></a>`).join('')}</nav>`;}
function renderContext(item){if((!item.context||!item.context.length)&&(!item.choices||!item.choices.length))return'';return`<section class="task-section panel"><p class="eyebrow">Background</p><h2>What you need to know first</h2>${(item.context||[]).map(s=>`<article class="context-card"><h3>${esc(s.title)}</h3>${block(s.body)}</article>`).join('')}</section>`;}
function bulletList(items,tone='success'){const marker=tone==='success'?'✓':'×';return`<ul class="result-list ${tone}">${(items||[]).map(v=>`<li><b>${marker}</b><span>${inline(v)}</span></li>`).join('')}</ul>`;}
function renderPreview(item){if(item.number!==1)return'';const src=`assets/starters/${esc(item.downloads.starterKey)}/index.html`;return`<section class="task-section panel"><p class="eyebrow">Live preview</p><h2>See the project before you touch the code.</h2><p class="task-section-intro">This is the starting version inside your backup project. It already renders the sample tickets. Your job in Stage 01 is to understand it and write the contract for the next feature, not to redesign the application yet.</p><div class="preview-window"><div class="preview-bar"><span></span><span></span><span></span><small>Campus ServiceDesk · starting state</small></div><iframe src="${src}" title="Live preview of the starting Campus ServiceDesk" loading="lazy"></iframe></div><div class="explain-grid"><article><span>Already works</span><b>12 sample tickets render from the project data.</b></article><article><span>You add today</span><b>A precise GitVerse issue that defines the next feature and how to test it.</b></article></div></section>`;}
export function renderTask(number){const item=runtime.course.find(x=>x.number===number);if(!item)return`<section class="empty panel"><h1>Stage not found</h1><a href="#tasks">Back to stages</a></section>`;if(item.locked)return`<header class="page-head"><p class="eyebrow">Stage ${pad(number)} of ${pad(runtime.course.length)}</p><h1>${esc(item.title)}</h1></header><div class="task-layout">${lessonNav(number)}<div class="task-main"><section class="task-section panel"><p class="eyebrow">Opens later</p><h2>The task is not available yet.</h2><p>${esc(item.releaseHint)}</p><p>The instructions and backup project files are not on the public site yet. They appear only when this stage opens.</p><a class="button" href="#tasks">Back to all stages</a></section></div></div>`;const done=doneSet().has(number),checked=taskSteps(number),prev=runtime.course[number-2],next=runtime.course[number];return`<header class="page-head"><p class="eyebrow">Stage ${pad(number)} of ${pad(runtime.course.length)} · 90 minutes</p><h1>${esc(item.title)}</h1><p class="lead">${esc(item.goal||item.ability)}</p></header><div class="task-layout">${lessonNav(number)}<div class="task-main"><section class="task-hero panel"><div class="actions"><a class="button primary" href="${esc(item.downloads.task)}" download>Full instructions</a><a class="button secondary starter-download" href="#" data-starter="${esc(item.downloads.starterKey)}" data-filename="${esc(item.downloads.starterFilename)}">Backup project</a><a class="button" href="${esc(item.downloads.report)}" download>Work report</a></div><div class="task-summary"><div class="summary-tile"><span>Branch</span><b><code>${esc(item.branch)}</code></b></div><div class="summary-tile"><span>GitVerse</span><b>${esc(item.gitverse)}</b></div><div class="summary-tile"><span>What you submit</span><b>${esc(item.artifact)}</b></div></div><p class="muted" style="margin-top:14px">Keep using your semester repository. The backup project is only for recovery after a missed stage or a broken local state.</p></section>${renderPreview(item)}${renderContext(item)}<section class="task-section panel"><p class="eyebrow">Work</p><h2>What to do</h2><ol class="steps">${item.steps.map((s,i)=>`<li class="${checked.has(i)?'checked':''}"><label><input type="checkbox" data-step="${i}" ${checked.has(i)?'checked':''}><span>${inline(s)}</span></label></li>`).join('')}</ol></section><section class="task-section panel success-panel"><p class="eyebrow">Completion check</p><h2>What counts as complete</h2><p class="task-section-intro">Every required check below must pass. “The AI said it works” is not evidence.</p>${bulletList(item.success,'success')}</section><section class="task-section panel failure-panel"><p class="eyebrow">Common false positives</p><h2>This does not count as complete</h2>${bulletList(item.failure,'failure')}</section><section class="task-section panel"><p class="eyebrow">Evidence</p><h2>What to submit</h2><div class="deliver-grid"><article class="deliver-card"><span>Main result</span><b>${esc(item.artifact)}</b></article>${item.minimum.map(v=>`<article class="deliver-card"><span>Evidence</span><b>${inline(v)}</b></article>`).join('')}</div></section><section class="task-section panel"><p class="eyebrow">Review</p><h2>What will be checked</h2><ul class="steps check-steps">${item.acceptance.map(v=>`<li><span>${inline(v)}</span></li>`).join('')}</ul></section>${item.defense?`<section class="task-section panel"><p class="eyebrow">Individual check</p><h2>Question you must answer yourself</h2><div class="question">${inline(item.defense)}</div></section>`:''}<section class="progress-action panel"><div><b>${done?'This stage is marked complete':'Did every required check pass?'}</b><p>This mark is local. The real evidence stays in GitVerse and in your working project.</p></div><button class="${done?'':'primary'}" id="toggle-done" data-number="${number}">${done?'Mark as not complete':'Mark complete'}</button></section><div class="task-controls"><span>${prev&&!prev.locked?`<a href="#task-${prev.number}">← ${esc(prev.title)}</a>`:''}</span><span>${next&&!next.locked?`<a href="#task-${next.number}">${esc(next.title)} →</a>`:'<a href="#tasks">All stages →</a>'}</span></div></div></div>`;}
export function bindTaskPage(route,rerender){document.querySelectorAll('[data-step]').forEach(input=>input.addEventListener('change',()=>{const n=Number(route.split('-')[1]);setTaskStep(n,Number(input.dataset.step),input.checked);input.closest('li')?.classList.toggle('checked',input.checked);}));document.querySelector('#toggle-done')?.addEventListener('click',e=>{const n=Number(e.currentTarget.dataset.number);setDone(n,!doneSet().has(n));rerender();});}
