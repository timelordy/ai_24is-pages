'use strict';
import {runtime,esc,pad,dateLabel,nearestLesson,taskCard} from './lib.js';
function nearestCard(n){if(n.intro)return`<section class="next-card panel"><div class="next-number">00</div><div><p class="eyebrow">Setup session · ${esc(n.group.room)}</p><h2>${esc(n.intro.title)}</h2><p>${esc(n.intro.note)}</p></div><a class="button primary" href="#current">Open setup guide</a></section>`;const lesson=n.lesson;if(lesson.locked)return`<section class="next-card panel"><div class="next-number">${pad(lesson.number)}</div><div><p class="eyebrow">Next scheduled stage · ${esc(n.group.room)}</p><h2>${esc(lesson.title)}</h2><p>${esc(lesson.releaseHint)}</p></div><a class="button" href="#tasks">See all stages</a></section>`;return`<section class="next-card panel"><div class="next-number">${pad(lesson.number)}</div><div><p class="eyebrow">Open now · ${esc(n.group.room)}</p><h2>${esc(lesson.title)}</h2><p><b>What you submit:</b> ${esc(lesson.artifact)}</p></div><a class="button primary" href="#task-${lesson.number}">Open stage</a></section>`;}
export function renderHome(){const n=nearestLesson(),opened=runtime.course.filter(x=>!x.locked).length;return`<section class="hero"><div class="hero-content"><p class="eyebrow">Semester software project</p><h1>One project.<br>Six stages.</h1><p class="lead">You keep the same Campus ServiceDesk repository for the whole semester. Each stage adds a real part of the system: the interface, an HTTP API, a database, business rules, team workflow, and finally a controlled AI feature.</p><div class="hero-actions"><a class="button primary" href="#current">Open current stage</a><a class="button secondary" href="#tasks">See project stages</a></div></div><div class="hero-metrics"><div class="metric"><strong>${opened}</strong><span>stage open now</span></div><div class="metric"><strong>6</strong><span>stages in total</span></div><div class="metric"><strong>1</strong><span>repository all semester</span></div><div class="metric"><strong>1</strong><span>working system at the end</span></div></div></section><div class="section-head"><div><p class="eyebrow">What to do next</p><h2>Your next session</h2></div><p>${esc(n.group.group)} · ${dateLabel(n.date)} · ${esc(n.group.time)}</p></div>${nearestCard(n)}<div class="section-head"><div><p class="eyebrow">How the course works</p><h2>Build, prove, merge, continue.</h2><p>Every stage starts with a clear expected result. AI can help with the work, but the result is accepted only when you can prove it with code, tests, and a working scenario.</p></div></div><section class="quick-grid"><article class="quick-card panel"><span>1</span><h3>Read the expected result</h3><p>Before asking AI for code, understand exactly what inputs and outputs must work.</p></article><article class="quick-card panel"><span>2</span><h3>Work in a branch</h3><p>Ask the coding assistant to inspect the project, make a plan, then change only what the task needs.</p></article><article class="quick-card panel"><span>3</span><h3>Prove it works</h3><p>Run the tests yourself, check the application, review the diff, then merge the branch only after every required check passes.</p></article></section>`;}
export const renderTasks=()=>`<header class="page-head"><p class="eyebrow">Project plan</p><h1>Six stages of one system.</h1><p class="lead">The cards below show the whole direction of the project. Only released stages contain instructions and files. A future stage shows its goal, but the actual task appears only when that stage opens.</p></header><section class="task-grid">${runtime.course.map(taskCard).join('')}</section>`;
export function renderCurrent(){const item=runtime.course.find(x=>!x.locked&&!runtime.state.done?.[runtime.state.group]?.includes(x.number))||runtime.course.filter(x=>!x.locked).at(-1);return`<header class="page-head"><p class="eyebrow">Current work</p><h1>${esc(item.title)}</h1><p class="lead">This is the stage you can work on now. Open it for the full task, the exact completion checks, and the backup project download.</p></header>${taskCard(item)}`;}
export function renderStart(){return renderCurrent();}
