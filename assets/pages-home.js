'use strict';
import {runtime,esc,pad,inline,dateLabel,nearestLesson,taskCard} from './lib.js';
function nearestCard(n){if(n.intro)return`<section class="next-card panel"><div class="next-number">00</div><div><p class="eyebrow">Вводная · ${esc(n.group.room)}</p><h2>${esc(n.intro.title)}</h2><p>${esc(n.intro.note)}</p></div><a class="button primary" href="#start">Настройка</a></section>`;const lesson=n.lesson;if(lesson.locked)return`<section class="next-card panel"><div class="next-number">${pad(lesson.number)}</div><div><p class="eyebrow">Следующая работа · ${esc(n.group.room)}</p><h2>🔒 ${esc(lesson.title)}</h2><p>${esc(lesson.releaseHint)}</p></div><a class="button" href="#tasks">Маршрут</a></section>`;return`<section class="next-card panel"><div class="next-number">${pad(lesson.number)}</div><div><p class="eyebrow">Открытая работа · ${esc(n.group.room)}</p><h2>${esc(lesson.title)}</h2><p><b>Milestone:</b> ${esc(lesson.artifact)}</p></div><a class="button primary" href="#task-${lesson.number}">Открыть</a></section>`;}
export function renderHome(){const n=nearestLesson(),opened=runtime.course.filter(x=>!x.locked).length;return`<section class="hero"><div class="hero-content"><p class="eyebrow">КГАСУ · ИСиТ · GitVerse + GigaCode</p><h1>Campus ServiceDesk: один проект на семестр</h1><p class="lead">Шесть milestones одного репозитория: frontend → REST API → SQLite/workflow → comments/process → AI triage. Сейчас открыто ${opened} из ${runtime.course.length}. Будущие TASK/checkpoints физически не опубликованы.</p><div class="hero-actions"><a class="button primary" href="#tasks">Открытые задания</a><a class="button secondary" href="#schedule">Расписание</a></div></div><div class="hero-metrics"><div class="metric"><strong>${opened}</strong><span>сейчас открыто</span></div><div class="metric"><strong>6</strong><span>milestones</span></div><div class="metric"><strong>1</strong><span>семестровый repo</span></div><div class="metric"><strong>4</strong><span>подгруппы</span></div></div></section><div class="section-head"><div><p class="eyebrow">Что делать сейчас</p><h2>Ближайшая встреча</h2></div><p>${esc(n.group.group)} · ${dateLabel(n.date)} · ${esc(n.group.time)}</p></div>${nearestCard(n)}<div class="section-head"><div><p class="eyebrow">Маршрут</p><h2>Семестровый проект</h2><p>Future cards видны, но содержание откроется только после предыдущего milestone.</p></div></div><section class="task-grid">${runtime.course.map(taskCard).join('')}</section>`;}
export const renderTasks=()=>`<header class="page-head"><p class="eyebrow">Campus ServiceDesk · 6 milestones</p><h1>Задания</h1><p class="lead">Открытая работа содержит TASK.md, Definition of Done и Recovery checkpoint. Будущая работа содержит только название и статус LOCKED.</p></header><section class="task-grid">${runtime.course.map(taskCard).join('')}</section>`;
export function renderStart(){const steps=[['Один repo на семестр','GitVerse Lab создаёт персональный Campus ServiceDesk. Не создавайте новый проект на каждую пару.','main хранит принятый результат предыдущих milestones.'],['Проверьте базу','Перед первой работой выполните команды.','npm test\nnpm run start'],['Читайте TASK до агента','Сначала Definition of Done, потом prompt.','Вы заранее знаете, какой output считается PASS.'],['Recovery — только аварийный путь','Если пропустили milestone или разрушили состояние, скачайте checkpoint текущей работы.','Будущие checkpoints не опубликованы.'],['Каждая feature через MR','Feature branch → tests → CI → review → merge в main.','Следующая работа начинается от накопленного main.']];return`<header class="page-head"><p class="eyebrow">Настройка один раз</p><h1>Как начать</h1><p class="lead">Это накопительный software project. Архив не заменяет персональный репозиторий.</p></header><section class="setup-list">${steps.map(([a,b,c])=>`<article class="setup-step panel"><div><h3>${esc(a)}</h3><p>${esc(b)}</p>${c.includes('\n')?`<pre class="command">${esc(c)}</pre>`:`<div class="expected"><b>Должно получиться:</b> ${inline(c)}</div>`}</div></article>`).join('')}</section>`;}
