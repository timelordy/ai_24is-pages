'use strict';
import {runtime,esc,pad,inline,dateLabel,nearestLesson,currentOpenLesson,taskCard} from './lib.js?v=99b18c7fecee';

function nearestCard(n){
  if(n.intro)return`<section class="next-card panel"><div class="next-number">00</div><div><p class="eyebrow">Вводная встреча</p><h2>${esc(n.intro.title)}</h2><p>${esc(n.intro.note)}</p></div><a class="button primary" href="#start">Открыть инструкцию</a></section>`;
  const lesson=n.lesson;
  if(lesson.locked)return`<section class="next-card panel"><div class="next-number">${pad(lesson.number)}</div><div><p class="eyebrow">Следующий этап</p><h2>${esc(lesson.title)}</h2><p>${esc(lesson.releaseHint)}</p></div><a class="button" href="#tasks">Посмотреть план проекта</a></section>`;
  return`<section class="next-card panel"><div class="next-number">${pad(lesson.number)}</div><div><p class="eyebrow">Текущий этап</p><h2>${esc(lesson.title)}</h2><p>${esc(lesson.artifact)}</p></div><a class="button primary" href="#task-${lesson.number}">Открыть этап</a></section>`;
}

export function renderHome(){
  const n=nearestLesson(),opened=runtime.course.filter(x=>!x.locked).length,current=currentOpenLesson();
  return`<section class="hero"><div class="hero-content"><p class="eyebrow">Семестровый проект</p><h1>Один проект.<br>Шесть этапов.</h1><p class="lead">Campus ServiceDesk развивается последовательно: сначала интерфейс, затем серверные запросы, база данных, история изменений, совместная проверка кода и функция с ИИ.</p><div class="hero-actions"><a class="button primary" href="#task-${current.number}">Открыть текущий этап</a><a class="button text" href="#tasks">Посмотреть все этапы →</a></div></div><div class="hero-metrics"><div class="metric"><strong>${opened}</strong><span>открыто сейчас</span></div><div class="metric"><strong>6</strong><span>этапов</span></div><div class="metric"><strong>1</strong><span>репозиторий</span></div><div class="metric"><strong>1</strong><span>итоговый продукт</span></div></div></section>
  <div class="section-head"><div><p class="eyebrow">Сейчас</p><h2>Ближайшая встреча</h2></div><p>${esc(n.group.group)} · ${dateLabel(n.date)} · ${esc(n.group.time)}</p></div>${nearestCard(n)}
  <div class="section-head"><div><p class="eyebrow">Как устроена работа</p><h2>От требования до доказанного результата</h2></div></div>
  <section class="quick-grid"><article class="quick-card panel"><span>1</span><h3>Точные требования</h3><p>Сначала записываем конкретные входные данные и ожидаемый результат. Фраза «должно работать» не считается проверкой.</p></article><article class="quick-card panel"><span>2</span><h3>Работа с GigaCode</h3><p>Помощник читает проект, предлагает план и делает только разрешённое изменение. Его ответы нужно проверять по реальным файлам.</p></article><article class="quick-card panel"><span>3</span><h3>Проверка</h3><p>Смотрим изменения Git, запускаем тесты и автоматическую проверку. Сообщение ИИ «готово» само по себе ничего не доказывает.</p></article></section>
  <div class="section-head"><div><p class="eyebrow">Термины</p><h2>Что означают слова, которые встретятся в заданиях</h2></div></div>
  <section class="quick-grid"><article class="quick-card panel"><span>Git</span><h3>Репозиторий и ветка</h3><p><b>Репозиторий</b> — папка проекта с историей изменений. <b>Ветка</b> — отдельная линия работы, чтобы не менять основную версию напрямую.</p></article><article class="quick-card panel"><span>MR</span><h3>Запрос на слияние</h3><p>Страница в GitVerse, где видно, что изменилось в ветке, прошли ли проверки и можно ли переносить результат в основную версию.</p></article><article class="quick-card panel"><span>CI</span><h3>Автоматическая проверка</h3><p>GitVerse сам запускает тесты после отправки кода. Зелёная проверка означает только то, что заданные тесты прошли, а не то, что программа идеальна.</p></article></section>
  <div class="section-head"><div><p class="eyebrow">План проекта</p><h2>Шесть этапов одного продукта</h2><p>Будущие этапы видны, но подробные задания и резервные копии открываются только после предыдущего занятия.</p></div><a href="#tasks">Все этапы →</a></div><section class="task-grid">${runtime.course.map(taskCard).join('')}</section>`;
}

export function renderTasks(){return`<header class="page-head"><p class="eyebrow">План проекта</p><h1>Этапы проекта</h1><p class="lead">Каждый этап продолжает один и тот же Campus ServiceDesk. Для открытого этапа доступны подробное задание, отчёт и резервная копия состояния проекта. Будущие материалы заранее не публикуются.</p></header><section class="task-grid">${runtime.course.map(taskCard).join('')}</section>`;}

export function renderCurrent(){const item=currentOpenLesson();return`<header class="page-head"><p class="eyebrow">Текущий этап</p><h1>${esc(item.title)}</h1><p class="lead">${esc(item.goal||item.ability)}</p><div class="actions"><a class="button primary" href="#task-${item.number}">Перейти к этапу ${pad(item.number)}</a><a class="button text" href="#tasks">План проекта →</a></div></header><section class="panel panel-pad"><p class="eyebrow">Что должно получиться</p><h2>${esc(item.artifact)}</h2><p class="muted">Основной путь — продолжать свой семестровый репозиторий. Резервная копия нужна только если проект потерян, сломан или предыдущий этап был пропущен.</p></section>`;}

export function renderStart(){const steps=[['Один репозиторий','Проект развивается весь семестр. Каждый новый этап продолжает результат предыдущего.','Основная ветка main содержит принятую версию проекта.'],['Проверьте проект','Перед работой запустите тесты и приложение.','npm test\nnpm run start'],['Сначала критерии','До запуска GigaCode прочитайте блок «Успешный результат».','Вы заранее знаете, какие конкретные проверки должны пройти.'],['Резервная копия только при необходимости','Если проект потерян или предыдущая работа пропущена, скачайте резервную копию текущего этапа.','Будущие состояния проекта недоступны.'],['Изменения через отдельную ветку','Работайте в отдельной ветке, проверьте изменения и только потом переносите результат в main.','Следующий этап начинается с принятой основной версии.']];return`<header class="page-head"><p class="eyebrow">Старт</p><h1>Настройте один раз.<br>Продолжайте весь семестр.</h1><p class="lead">Campus ServiceDesk не пересоздаётся от занятия к занятию.</p></header><section class="setup-list">${steps.map(([a,b,c])=>`<article class="setup-step panel"><div><h3>${esc(a)}</h3><p>${esc(b)}</p>${c.includes('\n')?`<pre class="command">${esc(c)}</pre>`:`<div class="expected"><b>Результат:</b> ${inline(c)}</div>`}</div></article>`).join('')}</section>`;}
