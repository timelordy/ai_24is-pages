'use strict';
import {runtime,esc,pad,inline,dateLabel,nearestLesson,currentOpenLesson,taskCard} from './lib.js';

function nearestCard(n){
  if(n.intro)return`<section class="next-card panel"><div class="next-number">00</div><div><p class="eyebrow">Вводная встреча</p><h2>${esc(n.intro.title)}</h2><p>${esc(n.intro.note)}</p></div><a class="button primary" href="#start">Открыть старт</a></section>`;
  const lesson=n.lesson;
  if(lesson.locked)return`<section class="next-card panel"><div class="next-number">${pad(lesson.number)}</div><div><p class="eyebrow">Следующий этап</p><h2>${esc(lesson.title)}</h2><p>${esc(lesson.releaseHint)}</p></div><a class="button" href="#tasks">Посмотреть маршрут</a></section>`;
  return`<section class="next-card panel"><div class="next-number">${pad(lesson.number)}</div><div><p class="eyebrow">Текущий этап</p><h2>${esc(lesson.title)}</h2><p>${esc(lesson.artifact)}</p></div><a class="button primary" href="#task-${lesson.number}">Открыть этап</a></section>`;
}

export function renderHome(){
  const n=nearestLesson(),opened=runtime.course.filter(x=>!x.locked).length,current=currentOpenLesson();
  return`<section class="hero"><div class="hero-content"><p class="eyebrow">Семестровый проект</p><h1>Один проект.<br>Шесть этапов.</h1><p class="lead">Campus ServiceDesk развивается последовательно: интерфейс, API, данные, workflow, командная работа и AI-функция.</p><div class="hero-actions"><a class="button primary" href="#task-${current.number}">Открыть текущий этап</a><a class="button text" href="#tasks">Посмотреть все этапы →</a></div></div><div class="hero-metrics"><div class="metric"><strong>${opened}</strong><span>открыто сейчас</span></div><div class="metric"><strong>6</strong><span>этапов</span></div><div class="metric"><strong>1</strong><span>репозиторий</span></div><div class="metric"><strong>1</strong><span>итоговый продукт</span></div></div></section>
  <div class="section-head"><div><p class="eyebrow">Сейчас</p><h2>Ближайшая встреча</h2></div><p>${esc(n.group.group)} · ${dateLabel(n.date)} · ${esc(n.group.time)}</p></div>${nearestCard(n)}
  <div class="section-head"><div><p class="eyebrow">Как устроена работа</p><h2>От идеи до проверенного результата</h2></div></div>
  <section class="quick-grid"><article class="quick-card panel"><span>1</span><h3>Контракт</h3><p>Сначала фиксируется, что именно считается правильным результатом.</p></article><article class="quick-card panel"><span>2</span><h3>Агент</h3><p>Потом AI получает контекст, предлагает план и делает ограниченное изменение.</p></article><article class="quick-card panel"><span>3</span><h3>Проверка</h3><p>Diff, тесты, CI и воспроизводимые сценарии важнее сообщения «готово».</p></article></section>
  <div class="section-head"><div><p class="eyebrow">Roadmap</p><h2>Шесть этапов одного продукта</h2><p>Будущие этапы видны, но их материалы появляются только после открытия.</p></div><a href="#tasks">Все этапы →</a></div><section class="task-grid">${runtime.course.map(taskCard).join('')}</section>`;
}

export function renderTasks(){return`<header class="page-head"><p class="eyebrow">Roadmap</p><h1>Этапы проекта</h1><p class="lead">Каждый этап продолжает один и тот же Campus ServiceDesk. Открытый этап содержит задачу и recovery checkpoint; будущие материалы не публикуются заранее.</p></header><section class="task-grid">${runtime.course.map(taskCard).join('')}</section>`;}

export function renderCurrent(){const item=currentOpenLesson();return`<header class="page-head"><p class="eyebrow">Текущий этап</p><h1>${esc(item.title)}</h1><p class="lead">${esc(item.goal||item.ability)}</p><div class="actions"><a class="button primary" href="#task-${item.number}">Перейти к этапу ${pad(item.number)}</a><a class="button text" href="#tasks">Roadmap →</a></div></header><section class="panel panel-pad"><p class="eyebrow">Результат этапа</p><h2>${esc(item.artifact)}</h2><p class="muted">Основной путь — продолжать свой семестровый репозиторий. Recovery checkpoint нужен только если состояние проекта потеряно или этап был пропущен.</p></section>`;}

export function renderStart(){const steps=[['Один репозиторий','Проект развивается весь семестр. Новый ZIP не заменяет ваш основной repo.','main содержит принятый результат предыдущих этапов.'],['Проверьте проект','Перед работой запустите тесты и приложение.','npm test\nnpm run start'],['Сначала критерии','Прочитайте Definition of Done до запуска агента.','Вы заранее знаете, какой результат считается PASS.'],['Recovery только при необходимости','Если состояние проекта потеряно, используйте checkpoint открытого этапа.','Будущие checkpoints недоступны.'],['Каждое изменение через MR','Feature branch → tests → CI → review → merge.','Следующий этап продолжает накопленный main.']];return`<header class="page-head"><p class="eyebrow">Старт</p><h1>Начните один раз.<br>Продолжайте весь семестр.</h1><p class="lead">Campus ServiceDesk не пересоздаётся от занятия к занятию.</p></header><section class="setup-list">${steps.map(([a,b,c])=>`<article class="setup-step panel"><div><h3>${esc(a)}</h3><p>${esc(b)}</p>${c.includes('\n')?`<pre class="command">${esc(c)}</pre>`:`<div class="expected"><b>Результат:</b> ${inline(c)}</div>`}</div></article>`).join('')}</section>`;}
